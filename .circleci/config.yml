version: 2.1
jobs:
  run:
   docker:
      - image: ubuntu:21.10

   steps:
      - run:
          name: setup config...
          command: |
           apt-get update && apt-get upgrade -y

workflows:
  version: 2.1
  cooking:
    jobs:
      - run
