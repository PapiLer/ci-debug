version: 2.1
jobs:
  run:
   docker:
      - image: hdrjt/aosp:latest

   steps:
      - run:
          name: setup config...
          command: |
           apt-get update && apt-get upgrade -y

workflows:
  version: 2.1
  cooking:
    jobs:
      - run
